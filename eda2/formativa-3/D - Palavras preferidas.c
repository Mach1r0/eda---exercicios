#include<stdio.h>
#include<stdlib.h>
#define MAX_WORD 200
typedef struct conjunto {
    int votos; 
    char palavra[MAX_WORD]; 
} conjunto; 

int main(){
    int command;
    char word[MAX_WORD];
    while (scanf("%d %d", &command, &word) != EOF){
        if (command == 1){
            conjunto.votos ++; 
            conjunto.palavra[]
        } 
        else if (command == 2){
            printf("%d\n", conjunto.votos);
        }
        else if (command = 3 )
        {
            /* code */
        }
        
        

    }
    
}